import { CandleInterval } from "./CommonTypes";
import { IStrategy } from "./Strategy";

// export interface StrategySignalInfo {
//   figi: string;
//   candleInterval: CandleInterval;

//   strategy: IStrategy;
//   time: number;
// }

// export interface IStrategySignalReceiver {
//   receive: (signal: SignalInfo) => void;
// }
